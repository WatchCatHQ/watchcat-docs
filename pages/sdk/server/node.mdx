import {Tab, Tabs} from 'nextra-theme-docs'
import Usage from '../../../components/usage_javascript.mdx'

# Node.js SDK

## Installation

To install the package, use your preferred package manager:

```shell
npm install @watchcathq/node
yarn add @watchcathq/node
```

To set up the package, import it and initialize it with your application token:

<Tabs items={['ESM', 'CommonJS']}>
    <Tab>
        ```javascript
        import WatchCat from "@watchcathq/node";

        WatchCat.init({
            token: "APP_TOKEN"
        })
        ```
    </Tab>
    <Tab>
        ```javascript
        const WatchCat = require("@watchcathq/node").default;

        WatchCat.init({
            token: "APP_TOKEN"
        })
        ```
    </Tab>
</Tabs>

## Logging events

<Usage/>


## Links

- __Github__: https://github.com/WatchCatHQ/watchcat-javascript/tree/main/packages/node
- __NPM__: https://www.npmjs.com/package/@watchcathq/node